// Legacy static app entry kept for backward compatibility with old static deployments.
// The active frontend is the Vue + Vite app mounted from `src/main.js`.
console.info("Use `npm run dev` to run the Vue app. frontend/app.js is legacy-only.");
